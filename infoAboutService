brigadeForAssign - сервис назначения бригад на вызов. Из АСОВ передаются параметры (pid Вызова, массив pid Бригад).

brigadesHistory - сервис истории нахождения бригад в определенный период времени. Из АСОВ передаются параметры (номера подстанций)

callHistory - сервис истории обслуживания вызова. Из АСОВ передаются параметры (pid Вызова).
              Отображаеся фактический маршрут обслуживаемой бригады, и предполагаемые маршруты записанные из сервиса brigadeForAssign.

factRouteHistory - сервис не используется.

heatMapForCall - сервис тепловой карты.

hospitalForAssign - сервис передачи параметров времени и расстояния доезда до вызова без использованием карты (в скрытом режиме)

medorg - сервис отображения медицинских организаций.

monitoring - сервис отображения бригад и вызовов в реальном времени. Из АСОВ передаются параметры (номера подстанций)

monitoringBrigadeOnCall - сервис доезда бригады на вызов. Из АСОВ передаются параметры ((pid Вызова, массив pid Бригады)

searchAddressForCall- сервис для регистрации вызова, для поиска координат вызова. Открывается в iframe АСОВ.